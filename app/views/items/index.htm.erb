<h1>These are the available items for sale</h1>

<div class="container">
<% @items.each do |item| %>
    <div class="item">
    <h4><%= item.name%></h4>
    <h4><%= item.description%></h4>
    <h4><%= "$#{item.price}"%></h4>
    <h4><%= item.location%></h4>
    </div>
<%end%>
</div>